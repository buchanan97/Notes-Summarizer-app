at http: tinyurl.com thinkpython2e . There are two versions of Python, called Python and Python . They are very similar, so if you learn one, it is easy to switch to the other. In fact, there are only a few differences you will encounter as a beginner. This book is written for Python , but I include some notes about Python . The Python interpreter is a program that reads and executes Python code. Depending on your environment, you might start the interpreter by clicking on an icon, or by typing python on a command line. When it starts, you should see output like this: Python (default, Jun 2015, : : ) GCC on linux Type "help", "copyright", "credits" or "license" for more information. The rst three lines contain information about the interpreter and the operating system it s running on, so it might be different for you. But you should check that the version number, which is in this example, begins with , which indicates that you are running Python . If it begins with , you are running (you guessed it) Python . The last line is a prompt that indicates that the interpreter is ready for you to enter code. If you type a line of code and hit Enter, the interpreter displays the result: Now you re ready to get started. From here on, I assume that you know how to start the Python interpreter and run code. . The rst program The rst program Traditionally, the rst program you write in a new language is called Hello, World! because all it does is display the words Hello, World! . In Python, it looks like this: