guration Of Foo install the stable version pharo Pharo.image con g MC REPOS URL Con guration Of Foo --install install a speci c version ' ' pharo Pharo.image con g MC REPOS URL Con guration Of Foo --install install a speci c version ' ' and only a speci c group 'Tests' pharo Pharo.image con g MC REPOS URL Con guration Of Foo --install -- group Tests Anatomy of a handler As we mentioned, the command line mechanism is open and can be extended. We will look now how at the handler for the eval option is de ned. Evaluating Pharo Expressions. You can use the command line to evaluate expressions as follows: . pharo Pharo.image eval ' ' . pharo Pharo.image eval --help Usage: eval --help smalltalk expression --help list this help message smallltalk expression a valid Smalltalk expression which is evaluated and the result is printed on stdout Documentation: A Command Line Handler that reads a string from the command line, outputs the evaluated result and quits the image. This handler either evaluates the arguments passed to the image: PHARO VM my.image eval or it can read directly from stdin: echo " " PHARO VM my.image eval Using Zero Conf script with Jenkins Now the handler is de ned as follows: First we de ne a subclass of Command Line Handler. Here Basic Code Loader is a subclass of Command Line Handler and Evaluate Command Line Handler is a subclass of Basic Code Loader . Basic Code Loader subclass: Evaluate Command Line Handler instance Variable Names: '' class Variable Names: '' pool Dictionaries: '' category: 'System -Command Line' We then de ne the command Name on the class