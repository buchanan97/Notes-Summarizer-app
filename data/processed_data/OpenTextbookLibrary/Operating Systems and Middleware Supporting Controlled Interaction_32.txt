of which I cover in Chapter . The les can be made a part of the process's virtual memory space, accessible with normal load and store instructions, or they can be treated separately, as external entities to read and write with explicit CHAPTER . INTRODUCTION operations. Either kind of le API provides a relatively simple interface to some quite signi cant mechanisms hidden within the operating system. Chapter also provides a survey of some of these mechanisms. As an example of a simple interface to a sophisticated mechanism, an application programmer can make a le larger simply by writing additional data to the end of the le. The