program exhibits some important features: . The sales array is of length and the variable number Months keeps track of how many months of data the user provides. . Prior to the while , in line , the rst sales amount is obtained . The while tests the value of the last sales amount obtained. . In the body of the while the previously obtained sales amount is placed into the array, and the next value is obtained. . Lines and accumulate the total sales . Testing for no months of data in line prevents the program from crashing if the user entered - as the rst value (division by zero). CHAPTER . ONE-DIMENSIONAL ARRAYS Arrays and Array Lists In some cases you may want to use the functionality of the Array List class but for whatever reason the data you are working with is in an array. It is easy to create an Array List from an array as shown in the program below. Listing : Initializing an Array List from an array. 1import java . util . Array List ; Create an Array List from an array of strings 5public class Array List From Array public static void main ( String args ) An array that will be used to provide initial data for an Array List String name "Joe "," Jasper "," Abigail " ; Array List String name AL new Array List ( name . length ); The add () method is used to append an element to the Array List for ( String n: name ) name AL .add(n); Printing an Array List results in each of its elements being displayed as in a comma