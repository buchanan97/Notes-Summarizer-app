select About. List of available handlers. The command line option --listlists of the current option handlers. This list depends on the handlers that are currently loaded in the system. In particular, it means that you can simply add a handler for your speci c situation and wishes. The following list shows the available handlers. . pharo Pharo.image --list Currently installed Command Line Handlers: st Loads and executes .st source les Fuel Loads fuel les con g Install and inspect Metacello Con gurations from the command line save Rename the image and changes le test A command line test runner update Load updates print Version Print image version eval Directly evaluates passed in one line scripts Loading Metacello Con guration. To get some explanation about the use of the con g option, just request its associated help as follows: . pharo Pharo.image con g --help Note that this help is the one of the associated handler, not one of the command line generic system. Usage: con g --help repository url con guration --install version -- group group --username username --password password --help show this help message repository url A Monticello repository name con guration A valid Metacello Con guration name version A valid version for the given con guration group A valid Metacello group name username An optional username to access the con guration's repository password An optional password to access the con guration's repository Zero Con guration Scripts and Command-Line Handlers Examples: display this help message pharo Pharo.image con g list all con gurations of a repository pharo Pharo.image con g MC REPOS URL list all the available versions of a confgurtation pharo Pharo.image con g MC REPOS URL Con