Workspace First Circle Pharo package -cache' cache parent full Name ' Users ducasse Workspace First Circle Pharo ' The methods exists ,is File ,is Directory , and basename are de ned on the File Reference class. Notice that there is no message to get the path without the basename and that the idiom is to use parent full Name to obtain it. The message path returns a Path object which is internally used by File System and is not meant to be publicly used. Note that File System does not really distinguish between les and folders which often leads to cleaner code and can be seen as an application of the Composite design pattern. Querying le entry status. To get additional information about a lesystem entry, we should get an File System Directory Entry using the message entry . Note that you can access the le permissions. Here are some examples: cache entry creation. 2012- - T15: : : cache entry creation Time 2012- - T15: : : cache entry creation Seconds 3512812296 2012 - - T14: : : cache entry modi cation Time 2012- - T14: : : cache entry size. (directories have size ) cache entry permissions rwxr-xr-x cache entry permissions class File System Permission cache entry permissions is Writable true cache entry is File false cache entry is Directory true Locations. The framework also supports locations, late-bound references that point to a le or directory. When asking to perform a concrete operation, a location behaves the same way as a reference. Here are some locations. File Locator desktop. File Locator home. File Locator image Directory. File Locator vm Directory. If you save a location with your image