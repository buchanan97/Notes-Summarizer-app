Arr ; int i; for(i ; i ; i) printf("Address of char Arr d u n", i, char Arr i ); return ; O P : - Address of char Arr 28ff44 Address of char Arr 28ff45 Address of char Arr 28ff46 Address of char Arr 28ff47 Program to find the sum of six numbers with arrays and pointers include stdio.h int main() int i, classes ,sum ; printf("Enter numbers: n"); for(i ; i ; i ) (classes i) is equivalent to classes i scanf(" d",(classes i)); (classes i) is equivalent to classes i sum (classes i); printf("Sum d" , sum); return ; Enter numbers: Sum C program to change the value of constant integer using pointers include stdio.h int main() const int a ; declare and assign constant integer int p; declare integer pointer p a; assign address into pointer p printf("Before changing - value of a: d",a); assign value using pointer p ; printf(" n After changing - value of a: d",a); printf(" n Wauuuu value has changed. "); return ; Before changing - value of a: After changing - value of a: Wauuuu value has changed. C program for array of pointers C program to demonstrate example of array of pointers. include stdio.h int main() declare same type of variables int a,b,c; we can create an integer pointer array to store the address of these integer variables int ptr ; assign the address of all integer variables to ptr ptr a; ptr b; ptr c; assign the values to a,b,c a ; b ; c ; print values using pointer variable printf("value of a: d, b: d, c: d n", ptr , ptr , ptr ); add