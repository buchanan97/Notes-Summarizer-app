the location that contains the desired value Declaration of Pointer to Pointer: int p2 Address Value Address p2 p1 Variable Chapter : Pointers Chapter :User -Defined Functions void main() intx,y; int p, z; x ; p x; y p; z p; clrscr(); printf ("Value of x is d n n",x); printf ("x d is stored at address x u n",x, x ); printf (" x d is stored at address x u n", x, x); printf (" p d is stored at address p u n", p,p); printf ("p u is stored at address p u n",p, p ); printf ("y d is stored at address y u n",y, y ); p ; printf (" n Now x d n",x); printf ("z u is stored at address z u n z u",z, z , z); getch (); Chapter : Pointers Chapter :User -Defined Functions Pointer Expressions Pointer variables can be used in expressions For example, p1 and p2 are declared and initialized pointer variables, then Y p1 p2 X ( p1) ( p2) p1 p2 invalid Chapter : Pointers Chapter :User -Defined Functions Pointer Increments and Scale Factor Pointers can be incremented like p1 p1 or p1 If p1 is an intpointer with initial value , say 2800,then after the operation p1 p1 , the value of p1 will 2802, not 2801 Chapter : Pointers Chapter :User -Defined Functions void main() intx ,y ,sum,sum1; int p, q; p x; q y; clrscr (); sum p q; printf ("sum d",sum ); printf (" np u q u", p,q); invalid pointer addition sum1 p q ; p p ; q; printf (" nx d p d q d np u q u",x,