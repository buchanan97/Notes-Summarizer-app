second and third elements to display() display(age Array , age Array ); return ; Passing arrays to functions include stdio.h float calculate Sum(float age ); int main() float result, age , , , , , ; age array is passed to calculate Sum() result calculate Sum(age); printf("Result f", result); return ; float calculate Sum(float age ) float sum ; for (int i ; i ; i) sum age i ; return sum; Passing two -dimensional arrays include stdio.h void display Numbers (int num ); int main() int num , i, j; printf("Enter numbers: n"); for (i ; i ; i) for (j ; j ; j) scanf(" d", num i j ); passing multi -dimensional array to display Numbers function display Numbers (num); return ; void display Numbers (int num ) Instead of the above line, void display Numbers(int num ) is also valid int i, j; printf("Displaying: n"); for (i ; i ; i) for (j ; j ; j) printf(" d n", num i j ); Output Enter numbers: Displaying: Passing Strings to Functions Strings can be passed to a function in a similar way as arrays. include stdio.h void display String(char str ); int main() char str ; printf("Enter string: "); gets(str ); display String(str); Passing string to a function. return ; void display String(char str ) printf("String Output: "); puts(str); Storage Classes in C auto : This is the default storage class for all the variables declared inside a function or a block. Hence, the keyword auto is rarely used while writing programs in C language. Auto variables can be only accessed within the block function they have been declar ed and not outside them (which