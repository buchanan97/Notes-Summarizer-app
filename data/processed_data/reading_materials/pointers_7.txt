main() int m , n ; printf("m d n", m); printf("n d n n", n); swap( m, n); passing address of m and n to the swap function printf("After Swapping: n n"); printf("m d n", m); printf("n d", n); return ; pointer 'a' and 'b' holds and points to the address of 'm' and 'n' void swap(int a, int b) int temp ; temp a; a b; b temp; Functions returning Pointer variables include stdio.h int larger(int , int ); void main() int a ; int b ; int p; p larger( a, b); printf(" d is larger", p); int larger(int x, int y) if( x y) return x; else return y; Pointer to functions type ( pointer -name)(parameter); int ( sum)(); legal declaration of pointer to function int sum(); This is not a declaration of pointer to function Example : include stdio.h int sum(int x, int y) return x y; void main( ) int ( fp)(int, int); fp sum; int s fp( , ); printf("Sum is d", s); Output : C program to read and print student details using structure pointer, demonstrate example of structure with pointer. include stdio.h struct student char name ; int roll; float perc; ; int main() struct student std; structure variable struct student ptr; pointer to student structure ptr std; assigning value of structure variable printf("Enter details of student: "); printf(" n Name ?:"); gets(ptr- name); printf("Roll No ?: "); scanf(" d", ptr- roll); printf("Percentage ?: "); scanf(" f", ptr- perc); printf(" n Entered details: "); printf(" n Name: s n Roll No: d n Percentage: .02f n",ptr- name,ptr- roll,ptr- perc); return ; O P:- Enter details of student: Name ?:Mike Roll No ?: