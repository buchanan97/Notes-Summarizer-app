are : d d ", u1. c, u1.d); return ; The Structure member values a and b are : The Union member values c and d are : Difference between structure and union Bit Field Declaration The declaration of a bit -field has the following form inside a structure struct type member name : width ; The following table describes the variable elements of a bit field Sr.No. Element Description type An integer type that determines how a bit -field's value is interpreted. The type may be int, signed int, or unsigned int. member name The name of the bit -field. width The number of bits in the bit -field. The width must be less than or equal to the bit width of the specified type. include stdio.h include string.h struct unsigned int age : ; Age; int main( ) Age.age ; printf( "Sizeof( Age ) : d n", sizeof(Age) ); printf( "Age.age : d n", Age.age ); Age.age ; printf( "Age.age : d n", Age.age ); Age.age ; printf( "Age.age : d n", Age.age ); return ; Sizeof( Age ) : Age.age : Age.age : Age.age :